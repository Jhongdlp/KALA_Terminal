name: CI

on:
  pull_request:
  push:
    branches: [main]

# A new push to a PR makes the previous run irrelevant.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: analyze & test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'
          channel: stable
          cache: true

      - name: flutter pub get
        run: flutter pub get

      # Infos (deprecated withOpacity, etc.) are pre-existing and tracked
      # separately; CI fails on warnings and errors only.
      - name: flutter analyze
        run: flutter analyze --no-fatal-infos

      - name: flutter test
        run: flutter test

      # Spanish is the source language; strings_en.dart is the table the
      # checker validates. A missing English entry falls back to Spanish,
      # so this reports rather than fails.
      - name: i18n coverage
        run: python3 scripts/i18n_check.py || true
